name: reverbeo
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.5.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  plugin_platform_interface: ^2.1.8

  biometric_auth_flutter:
    git:
      url: https://github.com/muhaiminkpm/biometric_auth_flutter.git
      ref: main
  provider: ^6.1.5+1
  record: ^6.1.0
  path: ^1.9.0
  path_provider: ^2.1.5
  audioplayers: ^6.4.0
  intl: ^0.19.0
  supabase_flutter: ^2.5.8


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true